name: PR Checks

on:
    pull_request:
        branches:
            - main

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v3
              with:
                  node-version: 20.x

            - name: Install and build
              run: yarn install && yarn run build
